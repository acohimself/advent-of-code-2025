[package]
name = "advent-of-code-2025"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.13.2", features = ["blocking"] }
ranges = { version = "0.4.0"}
good_lp = { version = "1.15.0", features = ["microlp"], default-features = false}
